# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.python-version
*.egg-info/

# CDK
cdk.out/
cdk.context.json
cdk-outputs.json
# tsc in-place output from `npm --workspace infra run build` (cdk runs via ts-node)
infra/**/*.js
infra/**/*.d.ts
# Local-only deploy config (real account/domain/zone) — never commit
infra/.deploy.env

# Operational scripts that act on the live stack — local-only, never public
scripts/lifecycle-test.py
scripts/create-admin-user.sh
scripts/db-cleanup-devices.py
scripts/provision-admin-user.py

# Env / secrets
.env
.env.local
.env.*.local
*-service-account.json
google-services.json

# OS / IDE
.DS_Store
.idea/
.vscode/
*.swp

# DB
*.sqlite
*.db
postgres-data/

# Logs
logs/
*.log

# Test
coverage/
.pytest_cache/
.coverage

# Lambda build artifacts
*.zip
.aws-sam/

# Working artifacts / planning / docs / generated dumps (local only)
Artifacts/
plans/
repomix-output.xml

# AI agent instructions — local-only, not for the public repo
CLAUDE.md
