# Copy to .deploy.env (gitignored) and fill with your own stack values.
# Loaded into process.env by bin/fluxion.ts at deploy time.
CDK_DEFAULT_ACCOUNT=<your-aws-account-id>
FLUXION_DOMAIN=<your-domain.example>
FLUXION_HOSTED_ZONE_ID=<route53-hosted-zone-id>
